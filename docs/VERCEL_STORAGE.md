# Deprecated — use [SUPABASE.md](SUPABASE.md)

Claycomp now uses **Supabase** as the primary storage backend on Vercel.

Upstash Redis is still supported as a legacy fallback if `UPSTASH_REDIS_REST_*` env vars are set and Supabase is not.
